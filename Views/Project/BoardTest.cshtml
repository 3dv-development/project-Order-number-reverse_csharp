@{
    ViewData["Title"] = "Board連携テスト";
}

<h2>Board連携テスト - 管理番号が空の案件一覧</h2>

<div class="alert alert-info">
    <strong>テスト目的:</strong> Board APIから管理番号が空の案件を取得し、案件名を表示します。
</div>

@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">
        <strong>エラー:</strong> @ViewBag.Error
    </div>
}

@if (ViewBag.Projects != null && ViewBag.Projects.Count > 0)
{
    <div class="mb-3">
        <strong>取得件数:</strong> @ViewBag.TotalCount 件
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>案件No</th>
                    <th>案件名</th>
                    <th>クライアント名</th>
                    <th>ステータス</th>
                    <th>管理番号</th>
                    <th>作成日</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var project in ViewBag.Projects)
                {
                    <tr>
                        <td><strong>@project.ProjectNo</strong></td>
                        <td>
                            @if (!string.IsNullOrEmpty(project.Name))
                            {
                                <span class="badge bg-success">✓</span>
                                @project.Name
                            }
                            else
                            {
                                <span class="text-muted">（案件名なし）</span>
                            }
                        </td>
                        <td>@project.ClientName</td>
                        <td>@project.OrderStatus</td>
                        <td>
                            @if (string.IsNullOrEmpty(project.ManagementNumber))
                            {
                                <span class="badge bg-warning">空欄</span>
                            }
                            else
                            {
                                @project.ManagementNumber
                            }
                        </td>
                        <td>@project.CreatedAt</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="mt-3">
        <p class="text-muted">
            <strong>確認事項:</strong><br>
            - 案件名に ✓ が表示されている案件は、Board APIから案件名が正しく取得できています。<br>
            - 管理番号が「空欄」の案件は、受注番号採番時に自動的に案件名とクライアント名が入力されます。
        </p>
    </div>
}
else
{
    <div class="alert alert-info">
        管理番号が空の案件がありません、またはBoard APIの認証情報が設定されていません。
    </div>
}

<div class="mt-4">
    <a href="/Project/Create" class="btn btn-primary">採番画面に戻る</a>
    <a href="/Home/Index" class="btn btn-secondary">ホームに戻る</a>
</div>
